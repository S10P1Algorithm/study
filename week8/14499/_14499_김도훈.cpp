#include <iostream>

using namespace std;

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	freopen("input.txt", "r", stdin);

	int N, M, x, y, K;
	cin >> N >> M >> x >> y >> K;
	for (int i = 0; i < N; i++) {

	}
	int* order = new int[K];
	for (int i = 0; i < K; i++) {
		cin >> order[i];
	}




	delete[] order;
	return 0;
}

